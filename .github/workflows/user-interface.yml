name: Deploy to GitHub Pages

on:
  push:
    branches:
      - lex_test 


jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
