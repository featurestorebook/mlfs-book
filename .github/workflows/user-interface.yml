name: Deploy to GitHub Pages

on:
  push:
    branches:
      - lex_test 


jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: stefanzweifel/git-auto-commit-action@v5
